function solve(args) {
    console.log(args[0].split('').reverse().join('')); 

    // var reversed = '';
    // for (var i = args[0].length - 1; i >= 0; i--) {
    //     reversed += args[0][i];
    // }
    // console.log(reversed);
}